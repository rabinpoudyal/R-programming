# integer - by default integer are stored as double so it makes sense to explicitly put L at end
a <- 3L
typeof(a)

#double => decimal
y <- 2.5
typeof(y)

#complex
z <- 3 +2i
type(z)

#character
a <- "h"
typeof(a)

#logical
q1 <- T
typeof(q1)
q2 <- FALSE
typeof(q2)
